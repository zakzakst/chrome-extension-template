# chrome-extension-template

## メモ

- ショートカットキーの割り当ては下記でできる
  - chrome://extensions/shortcuts

## その他やりたいこと

## アイデア

- 特定テキストを自動コピー
  - オプションページでセレクタのセットを設定
  - サイドパネルにセット名の一覧を表示
  - セット名をクリックすると、対応するセレクタで取得される内容が一覧表示される
- ページ目次作成
  - ページ内のheading要素を取得してサイドパネルに一覧表示
  - クリックするとその位置までスクロールする
- 画像サイズ確認（画像dimensions表示）
- 前に作ったサイドパネルメモの改善案
  - テンプレート挿入にスレッドシート連携の処理組み込む（スプレッドシート連携よりも、オプションページの入力を充実させたい ※以下アイデア）
    - 入力
    - 編集
    - 削除
    - 順番入れ替え（ドラッグ＆ドロップ）
    - csvでの内容ダウンロード
    - csv読込での内容追加
  - サイドパネルメモの拡張機能、コード量増えてるので、コンポーネント分割するプロンプトの作成も一緒に試す

## プロンプト周りもろもろ

- プロンプトで値を指定したほうがいい箇所の知見をためる
  - 今回でいうとstorageの要件定義を日本語で記載したらそのままstorage keyとして利用された
- レビューすることを見据えたコードの出力プロンプト
  - 自分でコードを書いていないため、処理の流れを追うのが大変だった。生成された後に自分がレビューしやすいような出力方法を考えてプロンプトに反映できるようにしたい
  - コード自体だけでなんとかするのではなく、レビュー補助プロンプトみたいなのの利用も頭の隅におきつつ考える
- 今回レビューしづらく感じた理由の一つに「コンポーネント分割していない」があると思う
  - アプローチとしては以下の2つが考えられる
    - コンポーネント分割を指定したプロンプトを書いて出力
    - 一度コンポーネント分割せずに出力して、生成されたものに対してコンポーネント分割する
  - 個人的には、初めからコンポーネント分割を見据えたプロンプトを書くのは難しい気がする（全体像をイメージできている必要があるので）
  - AIの「素早くたたき台を作れる」「たたき台があると全体像のイメージのとっかかりになる」という良さを活かすなら「一度コンポーネント分割せずに出力して、生成されたものに対してコンポーネント分割する」のほうが良い気がする。とりあえずこっちの方向性を探ってみる
- 最初に「構成要素」をリストアップしたのは上手くいったと思う。そのあとの「挙動」を記述するのが楽だった
  - これに限らず、**まず名前を付ける**のはプロンプトを書く際にやりやすいアプローチかなと感じた
- データに関しても一意に指定できる状態だとプロンプト書きやすいか？
  - 具体的には下記。ただ、入れ子のデータに関してはデータを分割したり、関数として抜き出したりとかで最終的にはシンプルにしたほうが良いと思う（一意に指定できるkeyでプロンプト ⇒ できたものをリファクタリングする際に長いkey名は修正 みたいな流れがいい？）
  - 自信はない。試す

```typescript
type BadData = {
  id: 1;
  name: "山田太郎";
  division: {
    id: 1;
    name: "○○部";
  };
};

type GoodData = {
  id: 1;
  name: "山田太郎";
  division: {
    divisionId: 1;
    divisionName: "○○部";
  };
};
```

- プロンプトを書く際、ライブラリ連携どう指定するか？今回はやらなかったshadcnなど、特有の記述方法がある場合にどのようにすれば上手くいくのか？

## タグ付けの方法

- git tag v0.0.0
  - タグが付与される
- git tag -a v0.0.0 -m "Release version 0.0.0"
  - メッセージ付きでタグを付与
- git tag
  - タグを確認できる
- git show v0.0.0
  - v0.0.0を見れる
- git push origin v0.0.0
  - v0.0.0をリモートリポジトリにpushする
- git push origin --tags
  - ローカルにあるタグ全てをリモートリポジトリにpushする
- git ls-remote --tags origin
  - リモートリポジトリのタグを確認する
- git tag -d v0.0.0
  - v0.0.0を削除
- git push origin --delete v0.0.0
  - リモートリポジトリのv0.0.0を削除（ローカルとリモートリポジトリでは別操作になる）
